/*
import Database from "../db/Database";
import Reader from "../model/Reader";

export default class ReaderController {

    private db!: Database;

    constructor(db: Database) {
        this.db = db;   
    }

    public getNewReader(): Reader {
        return new Reader();
    }
}*/